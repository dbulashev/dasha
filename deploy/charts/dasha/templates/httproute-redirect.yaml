{{- $redirectSection := include "dasha.redirectSectionName" . }}
{{- if and .Values.gatewayAPI.enabled .Values.gatewayAPI.tls.enabled .Values.gatewayAPI.tls.redirect $redirectSection }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "dasha.fullname" . }}-redirect
  namespace: {{ include "dasha.namespace" . }}
  labels:
    {{- include "dasha.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ include "dasha.routeGatewayName" . }}
      namespace: {{ include "dasha.routeGatewayNamespace" . }}
      sectionName: {{ $redirectSection }}
  hostnames:
    - {{ .Values.gatewayAPI.hostname }}
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
{{- end }}
